# M9 acceptance contract — slotted pages, row codec and heap file

Status: **implementation complete, external Windows R3 acceptance pending**.

Acceptance is performed only by the single cumulative root launcher:

```powershell
.\test.ps1 -Compiler C:\path\to\mlc_win64.py
```

The gate reruns all accepted M0-M5 regressions before this milestone. M9 requires:

- stable slots, compaction, delete/reuse and generation-protected RowIds;
- byte-atomic failure paths and permanent retirement of saturated generations;
- SQL NULL distinct from MiniLang void;
- row round trips across supported M9 scalar/text/binary/temporal types;
- heap insert/read/update/delete/reopen and stale-reference protection.

The milestone is accepted only when the cumulative result JSON marks `M9` `PASS`, the
launcher exits with code 0, no test is skipped and the final line is exactly:

```text
MiniSQL M6-M10R3 acceptance test: SUCCESS
```
