# M39 candidate — INSERT INTO ... SELECT

SELECT output is bound to target columns and completely materialized before any
target mutation. Count/type mismatch, self-insert finiteness, defaults,
RETURNING and rollback are tested.
