# M20 acceptance candidate

M20 adds offline consistency checking, verified directory backup/restore and fail-closed migration planning. The page-size migration tool never edits an existing database in place and refuses unsupported rewrites before mutation.
