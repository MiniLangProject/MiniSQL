# M3 acceptance contract

Status: **accepted by the cumulative M2–M5R2 Windows result (19/19 phases PASS)**.

Acceptance is performed by the single cumulative `test.ps1` M0-M5 gate.

M3 introduces the synchronous Win32 random-access file layer, explicit durable flush,
truncate, whole-file exclusive locks and a monotonic clock wrapper.

M3 acceptance includes every M2 gate and additionally requires:

- exact offset reads and writes, sparse-file behavior, append and truncate;
- persistence across independent writer and reader processes;
- explicit `FlushFileBuffers` success before durable acknowledgement;
- closed-handle rejection;
- a two-process exclusive-lock contention test; and
- the cumulative result marking M3 `PASS`, exit code 0 and final line `MiniSQL M2-M5 acceptance test: SUCCESS`.
