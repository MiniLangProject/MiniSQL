# M8 acceptance contract — database directories, configuration and catalog

Status: **implementation complete, external Windows R3 acceptance pending**.

Acceptance is performed only by the single cumulative root launcher:

```powershell
.\test.ps1 -Compiler C:\path\to\mlc_win64.py
```

The gate reruns all accepted M0-M5 regressions before this milestone. M8 requires:

- strict JSON parsing and fail-closed pre-DCL validation;
- global databaseDefaults copied only when a database is created;
- persistence and reopening of page size, database UUID, object IDs and transaction IDs;
- table/column catalog metadata and exactly one initial physical table file;
- rejection of missing table files and foreign catalog identity.

The milestone is accepted only when the cumulative result JSON marks `M8` `PASS`, the
launcher exits with code 0, no test is skipped and the final line is exactly:

```text
MiniSQL M6-M10R3 acceptance test: SUCCESS
```
