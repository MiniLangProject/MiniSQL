# M45 candidate — sequences, generated columns and triggers

M45 adds persistent named sequences with `NEXTVAL`/`CURRVAL`, stored generated
columns, and basic row-level `AFTER INSERT`, `AFTER UPDATE`, and `AFTER DELETE`
triggers. Sequence advancement is durable and non-transactional. Trigger bodies
are one DML statement and recursion is bounded.
