# M7 acceptance contract — checkpoint and crash recovery

Status: **implementation complete, external Windows R3 acceptance pending**.

Acceptance is performed only by the single cumulative root launcher:

```powershell
.\test.ps1 -Compiler C:\path\to\mlc_win64.py
```

The gate reruns all accepted M0-M5 regressions before this milestone. M7 requires:

- redundant checkpoint slots, monotonic generations and damaged-newest fallback;
- rejection of contradictory equal generations and invalid redo boundaries;
- committed-only full-page redo with pageLSN idempotence;
- restart recovery and torn WAL-tail handling;
- separate processes terminated immediately after committed and uncommitted WAL writes.

The milestone is accepted only when the cumulative result JSON marks `M7` `PASS`, the
launcher exits with code 0, no test is skipped and the final line is exactly:

```text
MiniSQL M6-M10R3 acceptance test: SUCCESS
```
