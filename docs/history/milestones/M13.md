# M13 acceptance — binding, types and SQL values

Status: **accepted** by `MiniSQL_M11_M15_RESULTS_20260726-143135.zip`. The cumulative
Windows run passed phase 32 after M11 and M12.

M13 verifies catalog-bound names, concrete SQL expression types and rejection of invalid
operations before execution. It covers the complete signed BIGINT range represented by
`Int64Words`, SQL NULL distinct from MiniLang `void`, three-valued Boolean logic, CHECK
semantics, LIKE, numeric promotion, length/precision validation and unknown-column or
type-mismatch errors. R1 reruns this accepted regression unchanged.
