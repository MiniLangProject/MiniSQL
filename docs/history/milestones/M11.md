# M11 acceptance — persistent B+ tree

Status: **accepted** by `MiniSQL_M11_M15_RESULTS_20260726-143135.zip`. The cumulative
Windows run passed phase 30, `M11 persistent B+ tree`, after all M0-M10 regressions.

The test created unique and non-unique indexes, inserted enough keys to form multiple
leaf pages and an internal root, verified ordered point/range lookup, rejected duplicate
unique keys, removed entries, validated the complete node graph, closed/reopened the
file and confirmed durable metadata and data. Every update published a new append-only
copy-on-write tree generation only after all new pages were durable. R1 reruns this
accepted regression unchanged.
