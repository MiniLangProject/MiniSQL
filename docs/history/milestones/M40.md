# M40 candidate — ON CONFLICT DO NOTHING

Conflict targets must exactly match a persisted primary-key or unique
constraint. Untargeted mode may suppress any such conflict. Other constraint
violations remain errors. Composite targets and NULL unique semantics are
covered.
