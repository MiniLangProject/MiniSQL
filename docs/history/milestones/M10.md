# M10 acceptance contract — overflow TEXT/BLOB storage

Status: **implementation complete, external Windows R3 acceptance pending**.

Acceptance is performed only by the single cumulative root launcher:

```powershell
.\test.ps1 -Compiler C:\path\to\mlc_win64.py
```

The gate reruns all accepted M0-M5 regressions before this milestone. M10 requires:

- zero-length, boundary, multi-page and multi-MiB values;
- full and ranged reads with whole-chain validation;
- owner, sequence, length, page type, cycle and missing-page detection;
- free-page reuse;
- two-phase replacement that keeps the old chain until explicit commit.

The milestone is accepted only when the cumulative result JSON marks `M10` `PASS`, the
launcher exits with code 0, no test is skipped and the final line is exactly:

```text
MiniSQL M6-M10R3 acceptance test: SUCCESS
```
