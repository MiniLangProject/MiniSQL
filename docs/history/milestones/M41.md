# M41 candidate — UPSERT

ON CONFLICT DO UPDATE supports target-row expressions, the synthetic `excluded`
row, optional WHERE, RETURNING, prepared statements, rollback and full final-row
constraint validation.
