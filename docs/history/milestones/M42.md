# M42 candidate — transactional TRUNCATE

TRUNCATE is implemented as WAL-backed staged deletion so it participates in
BEGIN/COMMIT/ROLLBACK. It reports affected rows, rejects referenced rows,
rebuilds derived indexes after commit, and restarts the current derived identity
model. CONTINUE IDENTITY remains unsupported before persistent sequences.
