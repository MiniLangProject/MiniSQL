# M33 acceptance candidate: SQL-aware shell and scripts

M33 passes when the lexical statement scanner handles multiline SQL, quoted
semicolons, comments, incomplete interactive suffixes and malformed finalized
input, and when the public `minisql.exe --script` executes a multiline
transaction through one real client/server session.
