# M21 acceptance candidate: DCL, authentication and authorization

> Superseded by `M21R1.md` after the first Windows candidate passed 49/52 phases
> and exposed the contextual-keyword identifier issue documented in
> `M21_FAILURE_20260727.md`.

M21 introduces users, roles, role membership, database and table privileges, password verifiers, authenticated loopback sessions, and security-aware backup/check tooling.

M21 remains a candidate until the cumulative Windows gate ends with:

```text
MiniSQL M21 acceptance test: SUCCESS
```

The single result archive is:

```text
build/MiniSQL_M21_RESULTS_<timestamp>.zip
```

The gate includes all accepted M0-M20 regressions plus security-catalog generation fallback, DCL authorization, challenge-response authentication, a real authenticated server/client process pair, backup/restore of security state, and the 71-module smoke test.
