# M44 candidate — CTEs and window functions

M44 adds materialized nonrecursive common table expressions and the window
functions `ROW_NUMBER`, `RANK`, `DENSE_RANK`, `COUNT`, `SUM`, `AVG`, `MIN`, and
`MAX` with `PARTITION BY` and `ORDER BY`. Recursive CTEs and explicit window
frames are rejected rather than approximated.
