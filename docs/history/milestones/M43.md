# M43 candidate — views and subqueries

M43 adds persisted `CREATE [OR REPLACE] VIEW`/`DROP VIEW`, scalar subqueries,
`EXISTS`, and `IN`/`NOT IN` subqueries. Subqueries are deliberately
non-correlated. A scalar subquery must produce one column and at most one row;
zero rows produce SQL `NULL`.
