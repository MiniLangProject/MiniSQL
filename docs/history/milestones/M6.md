# M6 acceptance contract — WAL and transaction core

Status: **accepted by `MiniSQL_M6_M10R2_RESULTS_20260726-115108.zip`; both M6 phases passed and the result JSON marks M6 `PASS`**.

Acceptance is performed only by the single cumulative root launcher:

```powershell
.\test.ps1 -Compiler C:\path\to\mlc_win64.py
```

The gate reruns all accepted M0-M5 regressions before this milestone. M6 requires:

- WAL record encode/decode, independent header and payload CRC-32C, ordered LSNs and invalid-tail repair;
- begin/page-image/commit/abort ordering;
- commit success only after the WAL flush boundary advances past the commit record;
- write and flush failure injection, transaction failure states and rollback;
- read-only enforcement, read-your-write semantics and database-level lock conflicts.

The milestone is accepted only when the cumulative result JSON marks `M6` `PASS`, the
launcher exits with code 0, no test is skipped and the final line is exactly:

```text
MiniSQL M6-M10R3 acceptance test: SUCCESS
```
