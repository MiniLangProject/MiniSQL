# M31 — WAL archive and PITR

Status in this archive: **acceptance candidate**.

Verified base backup, complete-prefix WAL generations, exact-LSN restore, offline standby materialization/refresh and explicit promotion.

M31 is accepted only when the cumulative Windows gate records `PASS`.
